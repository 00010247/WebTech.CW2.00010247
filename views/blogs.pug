extends layout.pug

block content

    div.container
        h1 All Blogs

        each blog in blogs
            div.blog-wrapper
                a.blog(href=`/blogs/${ blog.id }`) #{ blog.title }
                a.delete(href="#") delete

